#include<stdio.h>

int main()
{
    int n,num;
    int arr[100];
    int key;
    int coutnt = 0;
    printf("Enter number of arrays :");
    scanf("%d",&num);
    while(count <num)
    {
   printf("Enter the number of elemtents :");
   scanf("%d",&n);
   printf("Enter %d elements \n",n);
   for(int i = 0;i<n ;i++){
       scanf("%d",&arr[i]);
   }
   printf("Entet the element to search :");
   scanf("%d",&key);
   
   int s = 0;
   int e = 2;
   int cmpr = 0;
   while(s<=e &&  e<=n){
       if(key > arr[n])
        {
            printf("Not fount ");
            break;
        }
       if(key<arr[e])
       {
           for(int i = s ;i<e ;i++)
           if(arr[i] == key){
            printf("present %d",cmpr);
            break;
            }
       }
       
       else {
        s = e;
        e +=2;
       }
       cmpr +=1;
   }
   count +=1;
  } 
}
